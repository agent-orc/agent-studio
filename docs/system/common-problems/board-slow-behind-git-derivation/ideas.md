# Ideas

- Keep AGT-2703 (ETag plus payload trim on the board endpoints) as the follow-up
  now that the index exists: the response body is the remaining cost once the
  derivation is gone.
- Watch the Admin performance panel's spawns-per-minute reading after adding a
  repository to the workspace; the cross-repository budget is two at a time and a
  large workspace lengthens the sweep rather than widening the fan-out.
