[Unit]
# Plain systemctl stop/restart is intentionally refused. The sanctioned helper
# signals an in-process drain and starts the unit again after all slots finish.
RefuseManualStop=true

[Service]
# A completed bounded drain exits successfully and must remain stopped until
# the deploy helper explicitly starts the replacement generation.
Restart=on-failure
